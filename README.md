- `main.py` - Приложение на FastAPI
- `database.py` - Класс для работы с DB
- `create_tables.sql` - SQL код для создания таблицы
- `data/students_grades.csv` - Данные полученные для работы
- `test/` - Тестовые csv и сам файлик для тестирования с помощью Pytest 

Чтобы проверить работо способность кода необходимо прописать:
```
docker-compose up --build
```
После можно обратиться к следующим эндпоинтам:
 - POST /upload-grades
```
curl -F "file=@data/students_grades.csv" http://localhost:8000/upload-grades
```

 - GET /students/more-than-3-twos
```
curl http://localhost:8000/more-than-3-twos
```
 - GET /students/less-than-5-twos`
```
curl http://localhost:8000/less-than-5-twos
```

Также написан небольшоой тест, который можно запустить так:
```
cd test
pytest
```
Используемые библиотеки и фреймворки
- fastapi - веб фреймворк, в котором сделал эндпоинты;
- pandas - использую для чтения и валидации данных;
- psycopg2 - драйвер базы данных используем его, так как задание ограничивает нас. (подключаемся к БД и выполняем SQL скрипты);
- python-multipart - эта библиотека нужна, чтобы сервер мог получить данные;
- uvicorn[standard] - сервер для  запуска FastAPI;


Буду рад получить от вас фидбэк! 🤗